---
name: 2026-09-06-rm-00011-r5-closeout
description: RM-00011 R5 local closeout with merged tips, review findings, verification evidence, and remaining acceptance blockers
metadata:
  type: doc
  status: 已交付
---

# RM-00011 R5 Closeout

## Current Verdict

The R5 implementation set is locally merged into each repository main branch and the reviewed P1 findings are closed. This is a local engineering closeout, not a final Workflow acceptance: the real 101-connection socket run and Workflow evidence write-back remain open.

## Merged Tips

| Repository | Final local main tip | Result |
| --- | --- | --- |
| Architecture | `5cc9cc5` | C-1/C-2/C-4 wire contracts merged |
| Runtime | `18b5feb` | R5 runtime, snapshot migration, mirror sync, legacy constructor compatibility, and serial ECS test guard merged |
| Server | `dfcfd4d` | owner-thread host, oracle log emission, observer lifecycle, and ordered supersession close merged |
| Client | `11e0713` | Runtime-only replica/session/bot path and scoped ClientRpc contract assertion merged |
| Game | `99bb857` | live evidence oracle, canonical counter checks, and formal query outcome validation merged |

No branch was pushed by this closeout.

## Verification Evidence

- Architecture: `node eng/verify-wire.mjs` and `node --test eng/verify-wire.mjs`: 34 passed; 7 wire contracts green.
- Runtime merged main: ECS 35 passed, Replication 221 passed, generator 9 passed, Username sample 6 passed; `spec-lint` green.
- Server merged main: process library 126 passed, wire 15 passed, host 45 passed, architecture 48 passed; `cargo build --locked`, `cargo clippy --locked --all-targets --all-features -- -D warnings`, `cargo fmt --all -- --check`, and `spec-lint` green.
- Client merged main: Replica 70 passed, Session 45 passed, Bot 18 passed. Client `spec-lint` reports only checkout symlink resolution warnings for `.claude/agents`, `.claude/skills`, and `.agents/skills`.
- Game merged main: `npm test` 87 passed and `spec-lint` green.

## Review Closure

- Runtime C-2 mirror drift was fixed by syncing the reserved-through tombstone rule and hash to Architecture.
- Runtime implementation now carries reserved-through tombstones, LWM1 version 3 snapshots, and foreign-instance rejection.
- Server observer close events now remove one observer by ID; the logical session is disconnected only after the last observer closes.
- Server supersession close is queued after pending frames, preserving `ConnectionSuperseded` ordering under backpressure.
- Game query outcome checks now inspect the formal `outcome` field instead of searching request IDs and details.
- Game real-log directory output was verified in the current Server candidate (`write_oracle_logs`); the earlier missing-directory finding applied to the stale Server main tip, not `dfcfd4d`.

## Remaining Blockers

1. Real socket acceptance is not executed. Required environment is still missing: `LUMIO_GAME_ROOT`, the Runtime DLL, and the Native SDK/ABI path.
2. Workflow status/evidence has not been written back or read back in this local closeout. R-00408, R-00392, R-00393, acceptance items, and RM-00011 final handback must be updated only after the real smoke evidence exists.
3. Architecture root contains pre-existing untracked `.tmp-acceptance-r5*` directories and an untracked `.sdd` report; they were not modified or deleted. The isolated Architecture candidate is wire-green.

## Completion Estimate

- Public contract and repository implementation: **100% locally merged**.
- Review and repository gates: **complete for the reviewed local tips**.
- Real integration acceptance: **not executed / blocked by environment**.
- Workflow closure and final RM-00011 handback: **not complete**.

Overall R5 should be reported as **implementation-complete but acceptance-incomplete**, not as fully done.

---
name: 2026-09-06-rm-00011-r5-closeout
description: RM-00011 R5 closeout with merged tips, review findings, and real socket acceptance evidence
metadata:
  type: doc
  status: 已交付
---

# RM-00011 R5 Closeout

## Current Verdict

The R5 implementation set is locally merged into each repository main branch, the reviewed P1 findings are closed, and the real two-Pack socket/browser acceptance is green locally. Workflow evidence is written back and read back: R-00408 is in acceptance with four passed native acceptance items; R-00392 and R-00393 are in review with their known gaps recorded.

## Merged Tips

| Repository | Final local main tip | Result |
| --- | --- | --- |
| Architecture | `9a22527` | C-1/C-2/C-4 wire contracts merged; closeout and independent review recorded |
| Runtime | `18b5feb` | R5 runtime, snapshot migration, mirror sync, legacy constructor compatibility, and serial ECS test guard merged |
| Server | `f031afb` | async superseded-connection retirement and deterministic bounded old-socket evidence read |
| Client | `5745d11` | deterministic per-bot transport yield and Welcome self-binding regression assertion |
| Game | `d3f6fe0` | browser WebSocket frame evidence capture and cross-process canonical event-set comparison |

No branch was pushed by this closeout.

## Verification Evidence

- Architecture: `node eng/verify-wire.mjs` and `node --test eng/verify-wire.mjs`: 34 passed; 7 wire contracts green.
- Runtime merged main: ECS 35 passed, Replication 221 passed, generator 9 passed, Username sample 6 passed.
- Server merged main: process library 126 passed, wire 15 passed, host 45 passed, architecture 48 passed.
- Client merged main: Replica 70 passed, Session 45 passed, Bot 18 passed.
- Game merged main: `npm test` 87 passed.
- Architecture `spec-lint` still reports 10 pre-existing findings (nine stale review links and the root `.sdd` report); they are explicitly retained and tracked rather than deleted.
- Final Server candidate: `cargo test --lib --tests` passed the 126 process/unit tests; the standalone acceptance test requires the full replay environment and was rerun through the launcher below.
- Final Client candidate: Bot Host Debug build passed with 0 warnings / 0 errors; session regression test passed.
- Final Game candidate: `npm test` 87 passed after the browser evidence and compare-runs changes.

## Review Closure

- Runtime C-2 mirror drift was fixed by syncing the reserved-through tombstone rule and hash to Architecture.
- Runtime implementation now carries reserved-through tombstones, LWM1 version 3 snapshots, and foreign-instance rejection.
- Server observer close events now remove one observer by ID; the logical session is disconnected only after the last observer closes.
- Server supersession close is queued after pending frames, preserving `ConnectionSuperseded` ordering under backpressure.
- Game query outcome checks now inspect the formal `outcome` field instead of searching request IDs and details.
- Game real-log directory output was verified in the current Server candidate (`write_oracle_logs`); the earlier missing-directory finding applied to the stale Server main tip, not `dfcfd4d`.

## Real Acceptance Evidence

- Launcher output: `.tmp-acceptance-r5-pack5/`.
- Pack A and Pack B each contain `round-1` and `round-2`; the top-level `verify-report.json` has `packA.ok=true`, `packB.ok=true`, and no failures.
- Each round has `server/server.ndjson`, `client/client.ndjson`, `client/playwright-network.ndjson`, `evidence.json`, `persist-snapshot.bin`, and `process-b/restore-result.json`.
- The real evidence includes 101 entities (100 bots + 1 player), Runtime-decoded `InputCommand`, 101 `OnChatMessage` RPCs, browser WebSocket observation, query outcomes, snapshot/restore, same-ID takeover with old input rejected, `ConnectionSuperseded`, and tombstone expiry.
- Chromium/Playwright was installed locally to execute the browser leg; no application credentials were written to the repository.

## Remaining Blockers

1. The direct Rust acceptance test without the launcher is blocked unless `LUMIO_GAME_ROOT` is set. The final launcher invokes the real process path and both Pack A/B formal oracles pass.
2. `node .spec/tools/spec-lint.mjs` still reports 10 pre-existing documentation/root findings; these are outside the R5 implementation change set and remain visible in the independent review.
3. Architecture root contains pre-existing untracked `.tmp-acceptance-r5*` directories and an untracked `.sdd` report; they were not modified or deleted.

## Completion Estimate

- Public contract and repository implementation: **100% locally merged**.
- Review and repository gates: **complete for the reviewed local tips**.
- Real integration acceptance: **complete locally; Pack A/B oracle green**.
- Workflow evidence closure: **written and read back; R-00408 acceptance, R-00392 review, R-00393 review**.
- Final RM-00011 handback: **recorded in the R-00408 evidence comment and local closeout report; two known gaps remain explicit**.

Overall R5 should be reported as **implementation-complete and locally acceptance-green; Workflow evidence is closed with two explicit environment/documentation gaps remaining**.

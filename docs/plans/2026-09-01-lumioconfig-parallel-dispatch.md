# LumioConfig 并行线派活提示词（与 RM-00011 零交集）

生成日期：2026-09-01
用途：新开会话，整段粘贴。与 RM-00011 执行编排（另一会话）并行，施工面无重叠。

---

```text
【身份与工作目录】
你是 LumioConfig 配表系统的建设会话。工作目录 ~/LumioGames，新仓建在 ~/LumioGames/LumioConfig。
架构仓 ~/LumioGames/LumioGameEngineArchitecture 只读参考：可以读、可以 git fetch，
但不得 checkout、不得 commit、不得 push、不得新建或修改其中任何文件。

【并行边界 —— 最高优先级，动手前先读】
另一会话正在执行 RM-00011「ECS Formal Entity and Chat Vertical Slice」的全量开发编排
（卡 R-00344–R-00359，外加 Runtime 地基 R-00149/150/152/172/178/189）。其施工面覆盖：
- 架构仓：docs/adr/ 与 .spec/decisions/（ADR 落号）、Schema 索引、ID Registry、Baseline、README、
  docs/reviews/ 进度报告、以及七仓镜像更新
- 五个实现仓：LumioServer、LumioGameRuntime、LumioClient、LumioGame、LumioNativeCore
- 它是 Workflow 的唯一写入方

你的硬禁区（碰任何一条 = 立即停下、标 BLOCKED、上报 Owner，不得自行绕过）：
1. 不在架构仓创建或修改任何文件；特别是不占用架构仓 ADR 编号——docs/adr/ 与 .spec/decisions/
   两个落点都禁。ADR 号在那边是合并时现查现占的，你占号必然与它抢。
2. 不修改上述五个实现仓的任何文件。
3. 不做任何 Workflow 写入：不建室、不建卡、不流转、不评论。交付以本地 markdown + git 提交为准，
   Workflow 登记攒着等 Owner 统一授权。
4. 不碰任何 contract-mirror/ 或 generated/ 目录（那是七仓镜像面，归对方）。
5. 任何需要架构仓配合的事（ADR 落号、公共契约变更、Baseline），一律写入「待并入清单」上报 Owner，
   不自行动手、不在本地替对方决定。

你的安全区：~/LumioGames/LumioConfig。该仓当前不存在，无人触碰，可放手施工。

【使命】
按已定稿的《LumioConfig 设计概要》，建起配表系统的阶段 0 决议与阶段 1 垂直切片前段。
配表是 GAS、存档、内容层的共同上游，且与 RM-00011 零交集——这正是它现在能独立推进的原因。

【真值优先级与必读】
1. 架构仓 docs/specs/2026-08-30-lumioconfig-design-overview.md —— 定稿设计概要，唯一执行真值。
   尤其 §4 功能模块 M1–M10、§5 TODO 顺序、§6 明确不做。
2. 架构仓 docs/specs/2026-08-30-config-table-architecture-decisions.md —— 裁决流水，理由与旧 ADR 对账。
3. 架构仓 .spec/knowledge/standards/ 下的 code-style / testing / workflow —— 只读遵守，不修改。
开场先 git fetch origin，以 origin/main 为唯一真值。
提交号、编号、计数、哈希一律现场实测读取，不信任何转述值——本提示词里没有写死的派生值，也不要凭记忆填。

【四条红线（定稿已锁，砍任何功能都不能碰）】
1. 表的真身是文本文件。Excel、网页编辑器都只是看和改的窗口，不是真身。
2. 内容指纹与存储格式无关：同一张表数值没变，存成文本还是二进制，指纹必须一样。
3. 一台服务器一个版本：开机是哪版就用到退役，永远不会一帧里一半新表一半旧表。
4. AI 没有上线按钮：能查、能改、能预演，「推给玩家」永远是人签字。

【阶段编排】

阶段 0 —— 六条设计决议（落新仓 docs/decisions/，暂不占架构仓 ADR 号）
内容见设计概要 §5 阶段 0 表：
  0-1 权威源与补丁通道（文本格式三纪律、双门）
  0-2 ID 命名空间与发号（扩展 ADR-007）
  0-3 内容指纹与数值规则（指纹构造、四态写法、排序、浮点规则、双语言对照测试集）
  0-4 产物容器与三端切分（四层清单、S/C/V 标签、披露门禁、签名防回滚）
  0-5 版本生命周期（实例绑定、开发 reload、备齐纪律、回放钉版）
  0-6 工具面契约（补丁格式、报错格式、AI 五动作）
执行要求：
- 0-3 必须补齐定稿时明确记为「今天没钉」的 Unicode 归一化规则，并给出 Rust/C# 双语言对照测试集。
- 六条文件彼此独立，可并行起草（各自独立 worktree）。
- 每条结尾写明「将来搬入架构仓时对应哪张 ADR」，并汇总进「待并入清单」。
- 在每条头部注明：本决议暂落本仓、不占架构仓 ADR 号，原因是并行期避免与 RM-00011 抢号。
  避免后人误以为流程遗漏。

阶段 1 —— 垂直切片前段（严格按设计概要 §5 阶段 1 顺序）
1. 建仓 + M1 最小版：目录结构、文本表格格式、表结构定义文件、墓碑文件、格式化工具、CI 强制。
   先放两三张真表（技能 / 效果 / 掉落）。
   验收：格式化跑两遍结果一致；乱格式文件 CI 拒收；两个补丁改同一张表的不同行，Git 能自动合并。
2. M2 机器门最小版：查错 + 结构化报错。报错首先是写给 AI 看的，AI 拿着能自己修好重提。
   验收：坏补丁拿到能定位到行列的报错；好补丁没人管也能合进去；单个补丁查错秒级完成。
3. M3 发号台（可与第 2 步并行，文件集独立）：名字 / 终身编号 / 座位号三层身份；
   补丁里只写名字不写编号，编号排队领。
   验收：并发提补丁不冲号；删过的编号永不再出现；改名后老存档照样能读。
4. M4 导表器最小版：文本产物 + 按 S/C/V 标签切三份 + 盖三个指纹（内容 / 包裹 / 底稿）。
   第一期不做增量编译，全量。
   验收：同样的源编译两次字节一模一样；客户端产物里物理上没有机密列；
   客户端引用服务器机密时直接编译报错。
5. M8 查表代码（Rust 一路、C# 一路，两路可并行）：TryGet 同步返回、绝不下载、绝不读盘；
   Prepare 进场景前备货。
   验收：Rust 和 C# 读同一份文件，查一万次结果一致。
6. M9 装载器与阶段 1 验收三条链 —— 本轮暂缓。
   理由：M9 的运行时落点在 LumioGameRuntime modules/config，该仓正被另一会话大规模施工，
   现在进去必然 rebase 冲突。等对方阶段结束、Owner 重新排期。
   本轮结束时把 M9 待办与接口需求写进「待并入清单」。

【执行纪律】
- TDD 铁律：没有先失败的测试，就没有生产代码。
- 并行子 Agent 各自独立 git worktree；文件集互不重叠才可并行，重叠必串行。
- 每步交回四件套：改动清单 / 验证证据（真实命令输出，不得只声称「已通过」）/ known gaps / 沉淀落点。
- 每步完成派 reviewer 两级审查（spec 合规 + 代码质量）；审查方在独立环境验证；
  派审后主 loop 不得在同一环境跑构建。
- 同一问题三次不过 → 停下质疑方案，升级 Owner，不要第四次硬试。
- 报告一律落本仓 docs/reviews/ 的 markdown，不以链接交付。

【须停下向 Owner 确认的】
1. 仓名与位置：默认 ~/LumioGames/LumioConfig；若 Owner 另有指定，以其为准。
2. 远程仓库创建与首次 push —— 对外动作，必须确认后才做。本地建仓、本地提交不需要。
3. 六条决议何时搬进架构仓占 ADR 号 —— 必须等另一会话 Phase 0 的四张契约卡全部合并完毕，
   否则抢号。届时由 Owner 统一安排。
4. Workflow 建室 / 建卡授权 —— 当前无授权，不得写入。
```

---

## 附：本提示词的边界依据

对方施工面（从其提示词逐条提取）与本线的重叠检查：

| 对方施工面 | 本线是否触碰 | 处置 |
|---|---|---|
| 架构仓 `docs/adr/` + `.spec/decisions/`（ADR 落号） | **否** | 六条决议改落新仓，不占号 |
| 架构仓 Schema 索引 / ID Registry / Baseline / README | 否 | 禁区 |
| 架构仓 `docs/reviews/` 进度报告 | 否 | 本线报告落新仓 |
| 七仓镜像 / `contract-mirror` / `generated` | 否 | 禁区 |
| LumioServer / GameRuntime / Client / Game / NativeCore | **否** | 禁区；M9 因此暂缓 |
| Workflow 写入（对方为唯一写入方） | **否** | 本线零写入 |
| Runtime `modules/config`（配表运行时落点） | 否 | M9 暂缓的直接原因 |

结论：**零重叠。** 唯一的时间耦合是六条决议搬入架构仓占 ADR 号，已推迟到对方 Phase 0 之后。
